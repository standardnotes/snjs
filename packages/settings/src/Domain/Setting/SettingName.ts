/**
 * Enumerate all Settings with their names as identifiers
 *
 * Extend by adding more enum members, add payload shape to
 * {@link SettingPayload}
 */
export enum SettingName {
  MfaSecret = 'MFA_SECRET',
}
