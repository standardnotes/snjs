{
  "name": "@standardnotes/domain-events",
  "version": "2.32.6",
  "engines": {
    "node": ">=16.0.0 <17.0.0"
  },
  "description": "Domain Events SDK used in SN projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "author": "Standard Notes",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "clean": "rm -fr dist",
    "prebuild": "yarn clean",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "test:unit": "jest spec --coverage --passWithNoTests"
  },
  "dependencies": {
    "@standardnotes/auth": "workspace:*",
    "@standardnotes/features": "workspace:*",
    "@standardnotes/scheduler": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
