{
  "name": "@standardnotes/features",
  "version": "1.14.0",
  "engines": {
    "node": ">=14.0.0 <17.0.0"
  },
  "description": "Features SDK for Standard Notes projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "clean": "rm -fr dist",
    "prestart": "yarn clean",
    "start": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json && node features-to-json.mjs && cp -r ./src/Static/ ./dist/static",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@standardnotes/auth": "3.8.3",
    "@standardnotes/common": "1.2.1"
  },
  "devDependencies": {
    "@standardnotes/config": "^2.2.0",
    "typescript": "^4.0.5"
  }
}
