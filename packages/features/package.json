{
  "name": "@standardnotes/features",
  "version": "1.35.2",
  "engines": {
    "node": ">=14.0.0 <17.0.0"
  },
  "description": "Features SDK for Standard Notes projects",
  "main": "dist/index.js",
  "author": "Standard Notes",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "clean": "rm -fr dist",
    "prestart": "yarn clean",
    "start": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json && node features-to-json.mjs",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@standardnotes/auth": "^3.17.9",
    "@standardnotes/common": "^1.16.2"
  },
  "devDependencies": {
    "@standardnotes/config": "^2.2.2",
    "typescript": "^4.5.5"
  }
}
