{
  "name": "@standardnotes/snjs-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.19.0 <16.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "test:unit": "lerna run test:unit --parallel",
    "test:e2e": "bash test.sh",
    "test:e2e:dev-server": "docker-compose up -d",
    "test:e2e:dev": "lerna run test:e2e --parallel",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "publish-packages": "lerna publish from-package --yes",
    "test:mocha": "node e2e-server.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.22",
    "eslint": "^7.13.0",
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
